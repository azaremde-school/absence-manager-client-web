import { ActionTree } from 'vuex';
import ITemplateStore from './template-store';
import IRootState from '../../root-state';

const actions: ActionTree<ITemplateStore, IRootState> = {
};

export default actions;
